---
status: accepted
---

# CSVの任意IDまたは問題文で更新対象を識別する

CSVの再取り込みで学習履歴を保持するため、同じ単語帳内で任意のID列を優先し、ID列を使わない場合は問題文の完全一致で同じカードを判定する。IDを必須にすると3列のCSVを手軽に取り込む用途に準備が増え、問題文だけでは問題文自体の修正を追跡できないため、この併用を選んだ。曖昧な一致は自動更新せずプレビューで示し、CSVから消えたカードは保持する。
