{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only the bundled main window may use local application commands.",
  "windows": ["main"],
  "permissions": []
}
